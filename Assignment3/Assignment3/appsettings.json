{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // IMPORTANT! Database is removed and replaced with template data. For actual prod/running the code, change this to a real connections string.
  "ConnectionStrings": {
    "PracticeDbContext": "server=sever-name.cloudapp.azure.com;Encrypt=False;uid=my_uid;database=my_database;pwd=password_here"
  }
}
